[package]
name = "sync_staking"
version = "0.1.0"
edition = "2024_07"
description = "Staking contracts for Sync Protocol"

[dependencies]
starknet = "2.11.4"
sync_core = { path = "../core" }
openzeppelin = "2.0.0"

[dev-dependencies]
snforge_std = "0.43.0"
assert_macros = "2.11.4"

[[target.starknet-contract]]
sierra = true

[scripts]
test = "snforge test"
